[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "comet-cli"
version = "1.0.0"
dependencies = [
    "ollama",
    "textual",
    "colorama"
]

[project.scripts]
comet = "comet.cli:main"